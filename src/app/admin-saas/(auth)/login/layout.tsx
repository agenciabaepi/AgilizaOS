/**
 * Layout específico para a página de login do admin
 * Este layout NÃO verifica autenticação, pois é a página de login
 */
export default function AdminLoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

